#include <iostream>
using namespace std;

int main()
{
    int age;
    cin >> age;
    if(age >= 18){
        cout << "eligible fot vote";
    }
    

    return 0;
}
// if condition to verify eligible for vote or not
#include <iostream>
using namespace std;

int main()
{
    int age;
    cin >> age;
    if(age >= 18){
        cout << "eligible fot vote";
    }
    else{
        cout << "not eligible for vote";
    }

    return 0;
}
